name: npm test

on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Build image
        run: docker build -t petlibro-mcp .

      - name: Run tests
        run: docker run petlibro-mcp npm test
